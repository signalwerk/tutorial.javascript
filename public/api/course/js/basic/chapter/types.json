[
  {
    "id": "overview",
    "title": "Einleitung",
    "task": {
      "instruction": "Definiere eine Variable mit dem Namen `test` und weise bei der Definition den String `'Hallo'` zu.",
      "check": "test && test.toLowerCase().includes('hallo')",
      "solution": "var test = \"Hallo\""
    }
  },
  {
    "id": "number",
    "title": "Number",
    "task": {
      "instruction": "Gebe mit der Funktion `print()` eine Zahl mit Kommastelle aus.\n**Info:** In Javascript werden Kommastellen mit dem Punkt `.` abgetrennt nicht mit einem Komma `,`.",
      "check": "hasText(item => /s*[0-9]+\\.[0-9]+s*/.test(`${item}`))",
      "solution": "print(12.5)"
    }
  },
  {
    "id": "string",
    "title": "String",
    "task": {
      "instruction": "Gebe mit der Funktion `print()` einen String aus.",
      "check": "hasText(item => typeof item === 'string')",
      "solution": "print(\"Hallo Welt!\")"
    }
  },
  {
    "id": "boolean",
    "title": "Boolean",
    "task": {
      "instruction": "Definiere eine Variable `yay` mit dem Wert `true` und eine Variable `nay` mit dem Wert `false`.",
      "check": "yay === true && nay === false",
      "solution": "var yay = true\nvar nay = false"
    }
  }
]
